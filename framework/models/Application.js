/**
 * @module Application
 */
define(['core/BaseModel'], function(BaseModel) {
    /**
     * @class Application
     * @extends BaseModel
     */
    var Application = BaseModel.extend({});

    return Application;

});